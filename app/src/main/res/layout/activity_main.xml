<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/ic_action_home_bg"
    android:padding="16dp"
    tools:context=".ui.activity.MainActivity">
    <ImageButton
        android:id="@+id/update"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:scaleType="fitXY"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:background="@color/transparent"
        android:src="@drawable/ic_version_update"/>
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_alignParentRight="true"
        android:layout_marginRight="200dp"
        android:layout_centerVertical="true">
        <ImageButton
            android:id="@+id/classes"
            style="@style/imgbtn"
            android:src="@drawable/ic_class_training"/>
        <ImageButton
            android:id="@+id/rede"
            style="@style/imgbtn"
            android:layout_marginTop="16dp"
            android:layout_marginBottom="16dp"
            android:src="@drawable/ic_read_training"/>
        <ImageButton
            android:id="@+id/train"
            style="@style/imgbtn"
            android:src="@drawable/ic_eye_training"/>
    </LinearLayout>
    <ImageView
        android:id="@+id/quit"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:background="@color/transparent"
        android:src="@drawable/ic_action_safe_quit"/>
</RelativeLayout>
